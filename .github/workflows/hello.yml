name: Hello GitHub Actions

on: 
  push: # Запускається при кожному пуші в репозиторій

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello, GitHub Actions!"

